version: '3.8'

volumes:
  db-data:

services:
  db:
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_DB=rts_database
      - POSTGRES_PASSWORD=password
    volumes:
      - db-data:/var/lib/postgresql/data
